<UserControl x:Class="ProbabilityTrades.UI.WPF.Views.UserControls.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:customcontrols="clr-namespace:ProbabilityTrades.UI.WPF.Views.CustomControls"
             mc:Ignorable="d">

    <StackPanel Width="250"
                Orientation="Vertical"
                VerticalAlignment="Center">

        <TextBlock Text="P R O B A B I L I T Y   T R A D E S"
                   FontFamily="Fonts/#Saira SemiBold"
                   FontSize="16"
                   HorizontalAlignment="Center"
                   Foreground="#959FAC"
                   Margin="0,5,0,0" />

        <!-- username -->
        <TextBlock Text="Username"
                   Foreground="DarkGray"
                   FontSize="12"
                   FontFamily="Montserrat"
                   FontWeight="Medium"
                   Margin="0 35 0 0"/>
        <TextBox x:Name="txtUsername"
                 Text="{Binding Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 FontSize="13"
                 FontFamily="Montserrat"
                 FontWeight="Medium"
                 Foreground="White"
                 CaretBrush="LightGray"
                 BorderBrush="DarkGray"
                 BorderThickness="0 0 0 1"
                 Height="28"
                 VerticalAlignment="Center"
                 Margin="0 5 0 0"
                 Padding="20 5 0 0">
            <TextBox.Background>
                <ImageBrush ImageSource="/Images/icon-user.png"
                            Stretch="None"
                            AlignmentX="Left"/>
            </TextBox.Background>
        </TextBox>

        <!-- password -->
        <TextBlock Text="Password"
                   Foreground="DarkGray"
                   FontSize="12"
                   FontFamily="Montserrat"
                   FontWeight="Medium"
                   Margin="0 35 0 0"/>
        <customcontrols:BindablePasswordBox Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"                         
                                            Height="28"
                                            Margin="0 5 0 0">
        </customcontrols:BindablePasswordBox>

        <!-- error message -->
        <TextBlock Text="{Binding ErrorMessage}"
                   Foreground="#D75960"
                   FontSize="12"
                   FontFamily="Montserrat"
                   FontWeight="Medium"
                   Margin="0 10 0 0"
                   TextWrapping="Wrap"/>

        <Button Style="{StaticResource ButtonStyle}"
                Command="{Binding LoginCommand}"
                Content="LOG IN"
                Margin="0 30 0 0">
        </Button>

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0 15 0 0">
            <TextBlock Text="Forgot Password?"
                       Foreground="DarkGray"
                       FontSize="12"
                       FontFamily="Montserrat"
                       FontWeight="Medium" />
            <TextBlock Text="Reset"
                       Foreground="White"
                       FontSize="12"
                       FontFamily="Montserrat"
                       FontWeight="Medium"
                       Margin="8 0 0 0"/>
        </StackPanel>

    </StackPanel>

</UserControl>